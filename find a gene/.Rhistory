aln <- read.fasta("aligned_sequences.fasta")
library(bio3d)
aln <- read.fasta("aligned_sequences.fasta")
library(bio3d)
# read your aligned FASTA file
aln <- read.fasta("aligned_sequences.fasta.aln-fasta")
# compute identity matrix
id_matrix <- seqidentity(aln)
# simple heatmap
heatmap(id_matrix, symm = TRUE)
library(bio3d)
# read your aligned FASTA file
aln <- read.fasta("1.aln-fasta")
library(bio3d)
# read your aligned FASTA file
aln <- read.fasta("1.aln-fasta")
# compute identity matrix
id_matrix <- seqidentity(aln)
# simple heatmap
heatmap(id_matrix, symm = TRUE)
library(bio3d)
# read your aligned FASTA file
aln <- read.fasta("1.aln-fasta")
# compute identity matrix
id_matrix <- seqidentity(aln)
# simple heatmap
heatmap.2(id_matrix,
trace = "none",
dendrogram = "none",
col = bluered(100),
margins = c(16, 16),
cexRow = 0.7,
cexCol = 0.7,
key.title = "Identity",
key.xlab = "Percent")
library(bio3d)
# read your aligned FASTA file
aln <- read.fasta("1.aln-fasta")
# compute identity matrix
id_matrix <- seqidentity(aln)
# simple heatmap
heatmap(id_matrix, symm = TRUE)
library(bio3d)
# read your aligned FASTA file
aln <- read.fasta("1.aln-fasta")
# compute identity matrix
id_matrix <- seqidentity(aln)
# simple heatmap
heatmap(id_matrix,
symm = TRUE,
margins = c(10, 14),
cexCol = 0.7,
cexRow = 0.8)
library(bio3d)
# read your aligned FASTA file
aln <- read.fasta("1.aln-fasta")
# compute identity matrix
id_matrix <- seqidentity(aln)
# simple heatmap
heatmap(id_matrix,
symm = TRUE,
margins = c(10, 15),
cexCol = 0.7,
cexRow = 0.8)
library(bio3d)
# read your aligned FASTA file
aln <- read.fasta("1.aln-fasta")
# compute identity matrix
id_matrix <- seqidentity(aln)
# simple heatmap
heatmap(id_matrix,
symm = TRUE,
margins = c(10, 13),
cexCol = 0.7,
cexRow = 0.8)
library(bio3d)
# read your aligned FASTA file
aln <- read.fasta("1.aln-fasta")
# compute identity matrix
id_matrix <- seqidentity(aln)
# simple heatmap
heatmap(id_matrix,
symm = TRUE,
margins = c(10, 10),
cexCol = 0.7,
cexRow = 0.8)
